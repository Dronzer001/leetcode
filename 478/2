class Solution {
public:
    int maxDistinct(string s) {
        int ans=0;
        unordered_map<char,int>mid;
        for(int i=0;i<s.length();i+=1){
            mid[s[i]]++;
            if(mid[s[i]]==1) ans+=1;
        }
        return ans;
    }
};
