#include<bits/stdc++.h>
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        int p=1;
        int max1=INT_MIN,min1=INT_MAX,curr=1;
        for(int i=0;i<nums;i++){
            curr=curr*nums[i];
            if(nums[i]<0) swap(max1,min1);
            max1=max(max1,curr);
            if(c)
            min1=min(nums[i]*min1,nums[i]);
        }
        return max1;
    }
};
