class Solution {
public:
    void sortColors(vector<int>& a) {
      int end=a.size()-1;
      int start=0,curr=0;
      while(curr<=end){
        if(a[curr]==0){
            swap(a[start],a[curr]);
            curr+=1;
            start+=1;
        }
        else if(a[curr]==2){
            swap(a[curr],a[end]);
            end-=1;
        }
        else{
            curr+=1;
        }
      }
    }
};
/*Algorithm:

If the current element is 0, swap it with the element at start and move both pointers forward.

If the current element is 2, swap it with the element at end and move end backward. Do not move current because the swapped element needs to be checked.

If the current element is 1, simply move the current pointer forward.
*/
