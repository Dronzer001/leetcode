class Solution {
public:
    bool validDigit(int n, int x) {
        bool ans=false;
        while(n>0){
            if(n<10 && n==x) return false;
            int mid=n%10;
            if(mid==x) ans=true;
            n/=10;
        }
        return ans;
    }
};
