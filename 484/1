class Solution {
public:
    int residuePrefixes(string s) {
        int n=s.length();
        unordered_map<char,int>mid;
        int ans=0;
        for(int i=0;i<n;i+=1){
            mid[s[i]]++;
            if(mid.size() == (i+1)%3) ans+=1;
        }
        return ans;
    }
};
