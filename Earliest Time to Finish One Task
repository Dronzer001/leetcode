class Solution {
public:
    int earliestTime(vector<vector<int>>& tasks) {
        int early=INT_MAX;
        for(int i=0;i<tasks.size();i++){
            early=min(early,tasks[i][0]+tasks[i][1]);
        }
        return early;
    }
};
