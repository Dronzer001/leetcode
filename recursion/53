class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int n=nums.size();
        int sum=nums[0],max1=nums[0];
        for(int i=1;i<n;i+=1){
            sum=max(sum+nums[i],nums[i]);
            max1=max(max1,sum);
        }
        return max1;
    }
};
